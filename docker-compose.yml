version: '3.8'

services:
  backend:
    container_name: restapi-java
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    networks:
      - pusak-net

networks:
  pusak-net:
    driver: bridge
